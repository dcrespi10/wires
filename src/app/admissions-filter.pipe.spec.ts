import { AdmissionsFilterPipe } from './admissions-filter.pipe';

describe('AdmissionsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AdmissionsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
